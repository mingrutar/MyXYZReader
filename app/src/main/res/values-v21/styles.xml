<resources>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme.Main." parent="@style/AppTheme">
<!-- for non-material theme       <item name="android:windowContentTransitions">true</item>   -->
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowExitTransition">@transition/change_image_transform</item>
        <item name="android:windowEnterTransition">@transition/change_image_transform</item>
    </style>

    <style name="AppTheme.Details" parent="@style/AppTheme">
        <item name="android:windowContentTransitions">true</item>
<!-- interfering with shared element   <item name="android:windowEnterTransition">@transition/slide_botton</item>  -->
    </style>
</resources>
